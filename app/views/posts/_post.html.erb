<div class="subtitles"><strong>title:</strong> <%= post.title %></div>
<div class="subtitles"><strong>created: </strong> <%= post.created_at.strftime("%m/%d/%y at %r") %></div>
<div class="subtitles"> <strong>last updated: </strong> <%= post.updated_at.strftime("%m/%d/%y at %r") %></div>
<div class="text"> <%= post.body %></div>
